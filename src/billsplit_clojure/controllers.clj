(ns billsplit-clojure.controllers
  (:require [billsplit-clojure.logic :as l]))

; receives peoples names and form params
; creates bill with people names
; add products to bill with the consumption
(defn create-bill
  []
  1)

; receives bill with products
; calculate how much each person has to pay
; add 10% if any
(defn calculate-bill
  []
  1)
